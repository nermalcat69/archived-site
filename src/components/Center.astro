---

---

<div class="center">
  <slot />
</div>

<style>
  .center {
    box-sizing: content-box;
    margin-inline: auto;
    width: calc(100% - (var(--space-xl) * 1.5));
    max-inline-size: 1000px;
    padding-inline: var(--space-2xl);
  }
</style>
